{
  "name": "interview-recorder",
  "version": "1.0.0",
  "description": "Ask questions, get recorded answers!",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "echo ${APIG_ID} && ENVIRONMENT={APIG_ID:$APIG_ID} webpack --mode development",
    "echo": "echo ${TEST}"
  },
  "author": "Jon Schwartz",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.21.1",
    "recordrtc": "^5.6.1"
  },
  "devDependencies": {
    "webpack": "^5.15.0",
    "webpack-cli": "^4.3.1"
  }
}
